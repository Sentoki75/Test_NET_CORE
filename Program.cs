using System;

namespace Test_NET_CORE
{
    class Program
    {
        private const string HelloMessage = "Hello World!, there we go";

        static void Main(string[] args) => Console.WriteLine(HelloMessage);
    }
}
